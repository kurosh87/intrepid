{
  "version": 2,
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/next",
      "config": {
        "installCommand": "yarn install",
        "buildCommand": "yarn build"
      }
    }
  ],
  "env": {
    "YARN_VERSION": "4.1.1"
  }
}